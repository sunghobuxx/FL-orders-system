# 회원 품목 셀프 추가 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 회원이 등록된 전체 품목을 보고 필요한 것을 직접 자기 발주 목록에 넣는다. 단가 없는 품목은 관리자 승인을 거친다.

**Architecture:** `restaurant_products` 에 출처(`added_by`)를 남겨 회원이 뺄 수 있는 것을 구분한다. 단가 없는 품목은 별도 표 `product_requests` 에 쌓고, 승인될 때만 `restaurant_products` 로 옮긴다. 발주 화면이 `restaurant_products` 를 그대로 읽기 때문에 대기 상태를 같은 표에 섞으면 안 된다.

**Tech Stack:** Next.js 15 (App Router, edge runtime), Supabase (Postgres + service role), Cloudflare Pages, vitest

## Global Constraints

- **실거래 데이터로 시험하지 않는다.** 단위 테스트는 가짜 db 객체로, DB 를 실제로 건드리는 검증은 **테스트 식당으로만** 한다.
  `organization_id = af6378e4-0000-0000-0000-000000000001`
  `restaurant_id   = 7306bb64-4596-4a5e-92ab-6e9dd9395238`
  다른 업체의 `restaurant_products` 는 어떤 단계에서도 만들거나 지우지 않는다.
- 운영 DB 는 Supabase 프로젝트 `nvpaggacvbotgqyxfdof` 하나뿐이고 로컬 dev 도 같은 DB 를 본다.
- 모든 라우트는 `export const runtime = 'edge'` 로 시작한다.
- **Cloudflare Pages 에서 대괄호 동적 경로로 가는 서버 액션 POST 는 404 가 된다.** 저장 동작은 API 라우트(`fetch`)로 한다. GET 은 멀쩡해서 로컬에서는 안 잡힌다.
- 어드민·회원 데이터 쓰기는 `createAdminClient()` (service role) 로 한다. 세션 클라이언트는 RLS 에 막혀도 조용히 빈 결과가 된다.
- Supabase insert/update 는 실패해도 예외를 던지지 않는다. **반드시 `error` 를 확인한다.**
- 회원 API 는 쿠키 세션과 `Authorization: Bearer` 를 **둘 다** 받아야 한다. 모바일 앱이 Bearer 를 쓴다 (`app/api/member/orders/route.ts` 참고).
- 시간은 KST. `new Date(Date.now() + 9 * 3600_000).toISOString().slice(0, 10)`
- 테스트: `cd apps/web && pnpm test` / 타입체크: `pnpm exec tsc --noEmit`
- `git push` 는 사용자가 지시할 때만 한다. 커밋까지만 한다.

---

## File Structure

**신규**

| 파일 | 책임 |
|---|---|
| `supabase/migrations/20260811000000_member_product_self_add.sql` | `restaurant_products` 출처 컬럼 · `product_requests` 표 |
| `apps/web/lib/products/self-add.ts` | 추가 가능 품목 고르기 · 단가 유무 판정 (순수 로직 분리) |
| `apps/web/lib/products/self-add.test.ts` | 위 단위 테스트 |
| `apps/web/app/api/member/products/route.ts` | 회원: 추가 가능 목록 조회 / 추가 / 제거 |
| `apps/web/app/member/order/AddProductPanel.tsx` | 회원 발주 화면의 「품목 추가」 |
| `apps/web/app/api/admin/products/requests/route.ts` | 관리자: 요청 승인 / 거절 |
| `apps/web/app/admin/products/requests/page.tsx` | 관리자 「품목 요청」 화면 |
| `apps/web/app/admin/products/requests/RequestPanel.tsx` | 위 화면 클라이언트 |

**수정**

| 파일 | 무엇을 |
|---|---|
| `apps/web/app/member/order/page.tsx` | 추가 가능 품목·요청 상태를 조회해 넘긴다 |
| `apps/web/app/member/order/OrderForm.tsx` | 「품목 추가」 패널 연결, 회원 추가분에 `×`, 단가 0 경고 |
| `apps/web/app/admin/products/page.tsx` | 「품목 요청」 링크 (대기 건수 표시) |

---

## Task 1: 데이터 모델

**Files:**
- Create: `supabase/migrations/20260811000000_member_product_self_add.sql`

**Interfaces:**
- Consumes: 없음
- Produces: `restaurant_products.added_by / added_by_user / added_at`, 표 `product_requests`

- [ ] **Step 1: 적용 전 현재 상태를 기록한다**

Supabase MCP `execute_sql` 로 실행하고 결과를 적어 둔다 (조회만):

```sql
select count(*) as 기존연결, count(distinct restaurant_id) as 업체수
from restaurant_products;
```

- [ ] **Step 2: 마이그레이션 파일을 만든다**

`supabase/migrations/20260811000000_member_product_self_add.sql`:

```sql
-- 회원이 발주 품목을 직접 추가한다.
--
-- 지금은 관리자가 업체별로 품목을 하나하나 등록해 줘야 해서, 회원은 자기 목록에 없는
-- 품목을 못 시킨다. 업체당 평균 14개만 열려 있는데 활성 품목은 137개다.
--
-- 회원이 넣은 것과 관리자가 넣은 것을 구분해야 한다. 관리자가 정해 준 기본 품목을
-- 회원이 빼 버리면 안 되기 때문이다.

alter table public.restaurant_products
  add column if not exists added_by      text not null default 'admin',
  add column if not exists added_by_user uuid,
  add column if not exists added_at      timestamptz not null default now();

do $$
begin
  if not exists (select 1 from pg_constraint where conname = 'restaurant_products_added_by_check') then
    alter table public.restaurant_products
      add constraint restaurant_products_added_by_check check (added_by in ('admin', 'member'));
  end if;
end $$;

-- 기존 538 건은 전부 관리자가 넣은 것이다. default 'admin' 이 그대로 맞다.

-- 단가가 없는 품목은 바로 열지 않고 여기 쌓는다.
--
-- restaurant_products 에 status 를 붙이지 않고 표를 따로 두는 이유:
-- 발주 화면이 restaurant_products 를 그대로 읽는다(app/member/order/page.tsx).
-- 대기 상태를 같은 표에 섞으면 아직 안 열린 품목이 발주 목록에 새어 나갈 수 있다.
-- 조회 한 줄만 빼먹어도 그렇게 된다.
--
-- 단가 없이 발주되면 명세서가 0 원으로 나간다. 2026-08-09 에 일회용 손장갑이 그랬다.
create table if not exists public.product_requests (
  id            uuid primary key default gen_random_uuid(),
  restaurant_id uuid not null references public.restaurants(id) on delete cascade,
  product_id    uuid not null references public.products(id)    on delete cascade,
  requested_by  uuid,
  requested_at  timestamptz not null default now(),
  status        text not null default 'pending',
  decided_at    timestamptz,
  decided_by    uuid,
  constraint product_requests_status_check check (status in ('pending', 'approved', 'rejected'))
);

-- 같은 품목을 여러 번 요청하지 못하게 한다. 처리된 건은 다시 요청할 수 있어야 하므로
-- pending 일 때만 막는다.
create unique index if not exists product_requests_pending_uniq
  on public.product_requests (restaurant_id, product_id)
  where status = 'pending';

create index if not exists product_requests_pending_idx
  on public.product_requests (status, requested_at desc)
  where status = 'pending';

alter table public.product_requests enable row level security;
-- 정책을 두지 않는다. 읽고 쓰는 것은 service role 뿐이라 RLS 를 지나간다.
```

- [ ] **Step 3: 마이그레이션을 적용한다**

Supabase MCP `apply_migration`, 이름 `member_product_self_add`.

- [ ] **Step 4: 적용 결과를 확인한다**

```sql
select
  (select count(*) from information_schema.columns
    where table_name='restaurant_products'
      and column_name in ('added_by','added_by_user','added_at')) as 새컬럼수,
  (select to_regclass('public.product_requests')::text) as 요청표,
  (select count(*) from restaurant_products where added_by = 'admin') as admin표시,
  (select count(*) from restaurant_products where added_by <> 'admin') as 그외;
```

Expected: 새컬럼수 3, 요청표 `product_requests`, admin표시 = Step 1 의 기존연결 수, 그외 0

- [ ] **Step 5: 커밋**

```bash
cd /Users/buxx-macmini/Developer/fruitlife
git add supabase/migrations/20260811000000_member_product_self_add.sql
git commit -m "feat: 회원 품목 셀프 추가용 출처 컬럼과 요청 테이블"
```

---

## Task 2: 추가 가능 품목 고르기 (순수 로직)

**Files:**
- Create: `apps/web/lib/products/self-add.ts`
- Create: `apps/web/lib/products/self-add.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `type CatalogProduct = { id: string; standard_name: string; category: string | null; default_unit: string }`
  - `type AddableProduct = CatalogProduct & { price: number | null; needsApproval: boolean }`
  - `buildAddable(all: CatalogProduct[], mineIds: Set<string>, pendingIds: Set<string>, priceOf: Map<string, number>): AddableProduct[]`
  - `canMemberRemove(addedBy: string): boolean`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/web/lib/products/self-add.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { buildAddable, canMemberRemove, type CatalogProduct } from './self-add'

const all: CatalogProduct[] = [
  { id: 'p1', standard_name: '깻잎',   category: 'vegetable', default_unit: 'box' },
  { id: 'p2', standard_name: '사과',   category: 'fruit',     default_unit: 'box' },
  { id: 'p3', standard_name: '시금치', category: 'vegetable', default_unit: 'ea' },
]

describe('buildAddable', () => {
  it('이미 내 목록에 있는 품목은 뺀다', () => {
    const got = buildAddable(all, new Set(['p1']), new Set(), new Map([['p2', 5000], ['p3', 3000]]))
    expect(got.map(p => p.id)).toEqual(['p2', 'p3'])
  })

  it('단가가 있으면 needsApproval 이 false 고 금액이 붙는다', () => {
    const got = buildAddable(all, new Set(), new Set(), new Map([['p1', 27000]]))
    const p1 = got.find(p => p.id === 'p1')!
    expect(p1.price).toBe(27000)
    expect(p1.needsApproval).toBe(false)
  })

  it('단가가 없으면 needsApproval 이 true 고 금액은 null — 0 원으로 보이면 안 된다', () => {
    const got = buildAddable(all, new Set(), new Set(), new Map())
    const p2 = got.find(p => p.id === 'p2')!
    expect(p2.price).toBeNull()
    expect(p2.needsApproval).toBe(true)
  })

  it('단가가 0 이면 없는 것으로 본다 — 0 원 명세서가 나가면 안 된다', () => {
    const got = buildAddable(all, new Set(), new Set(), new Map([['p2', 0]]))
    const p2 = got.find(p => p.id === 'p2')!
    expect(p2.price).toBeNull()
    expect(p2.needsApproval).toBe(true)
  })

  it('이미 요청해 둔 품목은 목록에서 뺀다 — 두 번 요청하면 헷갈린다', () => {
    const got = buildAddable(all, new Set(), new Set(['p2']), new Map())
    expect(got.map(p => p.id)).toEqual(['p1', 'p3'])
  })

  it('분류 없는 품목도 빠뜨리지 않는다', () => {
    const odd: CatalogProduct[] = [{ id: 'p9', standard_name: '기타', category: null, default_unit: 'ea' }]
    const got = buildAddable(odd, new Set(), new Set(), new Map([['p9', 1000]]))
    expect(got.map(p => p.id)).toEqual(['p9'])
  })
})

describe('canMemberRemove', () => {
  it('회원이 넣은 것은 뺄 수 있다', () => {
    expect(canMemberRemove('member')).toBe(true)
  })

  it('관리자가 넣은 기본 품목은 못 뺀다', () => {
    expect(canMemberRemove('admin')).toBe(false)
  })

  it('값이 없으면 관리자 것으로 보고 못 빼게 한다 — 안전한 쪽으로', () => {
    expect(canMemberRemove('')).toBe(false)
  })
})
```

- [ ] **Step 2: 테스트가 실패하는지 확인한다**

Run: `cd apps/web && pnpm exec vitest run lib/products/self-add.test.ts`
Expected: FAIL — `Failed to resolve import "./self-add"`

- [ ] **Step 3: 모듈을 만든다**

`apps/web/lib/products/self-add.ts`:

```ts
/**
 * 회원이 직접 추가할 수 있는 품목 고르기.
 *
 * db 를 건드리지 않는 순수 함수로 둔다. 규칙이 잘못되면 회원 발주 목록에 엉뚱한 품목이
 * 뜨거나 0 원 명세서가 나가므로, 그대로 시험할 수 있어야 한다.
 */

export interface CatalogProduct {
  id: string
  standard_name: string
  category: string | null
  default_unit: string
}

export interface AddableProduct extends CatalogProduct {
  /** 기본 단가. 없으면 null — 0 으로 두면 화면에 "0원" 으로 보인다 */
  price: number | null
  /** 단가가 없어 관리자 확인을 거쳐야 하는 품목 */
  needsApproval: boolean
}

/**
 * @param all        활성 품목 전체
 * @param mineIds    이미 내 목록에 있는 품목
 * @param pendingIds 이미 요청해 둔 품목
 * @param priceOf    품목별 기본 단가
 */
export function buildAddable(
  all: CatalogProduct[],
  mineIds: Set<string>,
  pendingIds: Set<string>,
  priceOf: Map<string, number>,
): AddableProduct[] {
  const out: AddableProduct[] = []
  for (const p of all) {
    if (mineIds.has(p.id)) continue
    if (pendingIds.has(p.id)) continue
    // 단가 0 은 "없음" 으로 본다. 그대로 발주되면 명세서가 0 원으로 나간다.
    const raw = priceOf.get(p.id)
    const price = raw !== undefined && raw > 0 ? raw : null
    out.push({ ...p, price, needsApproval: price === null })
  }
  return out
}

/** 관리자가 정해 준 기본 품목은 회원이 빼면 안 된다. 값이 없으면 안전한 쪽으로 본다. */
export function canMemberRemove(addedBy: string): boolean {
  return addedBy === 'member'
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd apps/web && pnpm exec vitest run lib/products/self-add.test.ts`
Expected: PASS 9 tests

- [ ] **Step 5: vitest 가 이 폴더도 보게 한다**

`apps/web/vitest.config.ts` 의 `include` 를 다음으로 바꾼다:

```ts
include: ['lib/**/*.test.ts'],
```

이미 `lib/**` 이므로 그대로면 된다. 전체 실행으로 확인한다.

Run: `cd apps/web && pnpm test`
Expected: self-add 9개를 포함해 전부 통과

- [ ] **Step 6: 타입체크하고 커밋**

Run: `cd apps/web && pnpm exec tsc --noEmit` → 0건

```bash
cd /Users/buxx-macmini/Developer/fruitlife
git add apps/web/lib/products/self-add.ts apps/web/lib/products/self-add.test.ts
git commit -m "feat: 회원이 추가할 수 있는 품목 고르기 규칙"
```

---

## Task 3: 회원 API

**Files:**
- Create: `apps/web/app/api/member/products/route.ts`

**Interfaces:**
- Consumes: `buildAddable`, `canMemberRemove` (Task 2), `buildPriceMapByProduct` from `@/lib/specs/sync`
- Produces:
  - `GET  /api/member/products?restaurantId=&businessDate=` → `{ addable: AddableProduct[], pending: {product_id, standard_name}[] }`
  - `POST /api/member/products` body `{ restaurantId, productIds: string[] }` → `{ added: string[], requested: string[] }`
  - `DELETE /api/member/products?restaurantId=&productId=` → `{ success: true }`

- [ ] **Step 1: 라우트를 만든다**

`apps/web/app/api/member/products/route.ts`:

```ts
export const runtime = 'edge'

import { NextRequest, NextResponse } from 'next/server'

import { createAdminClient } from '@/lib/supabase/admin'
import { getSessionUser } from '@/lib/supabase/server'
import { fetchAll } from '@/lib/supabase/fetch-all'
import { buildPriceMapByProduct } from '@/lib/specs/sync'
import { buildAddable, canMemberRemove, type CatalogProduct } from '@/lib/products/self-add'

/**
 * 회원이 자기 발주 품목을 직접 넣고 뺀다.
 *
 * 단가가 있는 품목은 바로 열고, 없는 품목은 product_requests 에 쌓아 관리자 확인을
 * 거친다. 단가 없이 발주되면 명세서가 0 원으로 나가기 때문이다
 * (2026-08-09 일회용 손장갑).
 *
 * 관리자가 넣어 준 기본 품목은 회원이 빼지 못한다.
 */

/** 쿠키 세션과 Bearer 토큰을 둘 다 받는다. 모바일 앱은 쿠키가 없다. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
async function resolveUserId(req: NextRequest, db: any): Promise<string | null> {
  const auth = req.headers.get('Authorization')
  const token = auth?.startsWith('Bearer ') ? auth.slice(7) : ''
  if (token) {
    const { data, error } = await db.auth.getUser(token)
    if (error || !data.user) return null
    return data.user.id
  }
  const { user } = await getSessionUser()
  return user?.id ?? null
}

/** 이 회원이 그 식당을 다룰 수 있는지. 없으면 null. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
async function assertAccess(db: any, userId: string, restaurantId: string) {
  const { data: restaurant } = await db
    .from('restaurants').select('id, organization_id').eq('id', restaurantId).maybeSingle()
  if (!restaurant) return null
  const { data: membership } = await db
    .from('memberships').select('organization_id')
    .eq('user_id', userId).eq('organization_id', restaurant.organization_id).maybeSingle()
  return membership ? restaurant : null
}

export async function GET(req: NextRequest) {
  try {
    const db = createAdminClient()
    const userId = await resolveUserId(req, db)
    if (!userId) return NextResponse.json({ error: '로그인이 필요합니다.' }, { status: 401 })

    const restaurantId = req.nextUrl.searchParams.get('restaurantId') ?? ''
    const businessDate = req.nextUrl.searchParams.get('businessDate') ?? ''
    if (!restaurantId || !/^\d{4}-\d{2}-\d{2}$/.test(businessDate)) {
      return NextResponse.json({ error: '식당 또는 발주 일자가 올바르지 않습니다.' }, { status: 400 })
    }

    const restaurant = await assertAccess(db, userId, restaurantId)
    if (!restaurant) return NextResponse.json({ error: '권한이 없습니다.' }, { status: 403 })

    const all = await fetchAll<CatalogProduct>(() => db
      .from('products').select('id, standard_name, category, default_unit').eq('status', 'active'))

    const mine = await fetchAll<{ product_id: string }>(() => db
      .from('restaurant_products').select('product_id').eq('restaurant_id', restaurantId))
    const mineIds = new Set(mine.map(m => m.product_id))

    const pendingRows = await fetchAll<{ product_id: string }>(() => db
      .from('product_requests').select('product_id')
      .eq('restaurant_id', restaurantId).eq('status', 'pending'))
    const pendingIds = new Set(pendingRows.map(r => r.product_id))

    const { priceMap } = await buildPriceMapByProduct(
      db, all.map(p => p.id), businessDate, restaurant.organization_id)
    const priceOf = new Map(Object.entries(priceMap).map(([k, v]) => [k, Number(v)]))

    const nameOf = new Map(all.map(p => [p.id, p.standard_name]))
    return NextResponse.json({
      addable: buildAddable(all, mineIds, pendingIds, priceOf),
      pending: [...pendingIds].map(id => ({ product_id: id, standard_name: nameOf.get(id) ?? '품목' })),
    })
  } catch (e) {
    console.error('[GET /api/member/products]', e)
    return NextResponse.json({ error: '품목을 불러오지 못했습니다.' }, { status: 500 })
  }
}

export async function POST(req: NextRequest) {
  try {
    const { restaurantId, productIds } = await req.json() as {
      restaurantId?: string; productIds?: string[]
    }
    if (!restaurantId || !Array.isArray(productIds) || productIds.length === 0) {
      return NextResponse.json({ error: '추가할 품목을 선택하세요.' }, { status: 400 })
    }

    const db = createAdminClient()
    const userId = await resolveUserId(req, db)
    if (!userId) return NextResponse.json({ error: '로그인이 필요합니다.' }, { status: 401 })

    const restaurant = await assertAccess(db, userId, restaurantId)
    if (!restaurant) return NextResponse.json({ error: '권한이 없습니다.' }, { status: 403 })

    // 오늘 기준 단가로 판정한다. 단가가 없는 것만 요청으로 보낸다.
    const today = new Date(Date.now() + 9 * 3600_000).toISOString().slice(0, 10)
    const { priceMap } = await buildPriceMapByProduct(db, productIds, today, restaurant.organization_id)

    const added: string[] = []
    const requested: string[] = []
    for (const id of productIds) {
      if (Number(priceMap[id] ?? 0) > 0) added.push(id)
      else requested.push(id)
    }

    if (added.length) {
      const { error } = await db.from('restaurant_products').insert(
        added.map(pid => ({
          restaurant_id: restaurantId,
          product_id: pid,
          added_by: 'member',
          added_by_user: userId,
        })))
      if (error) return NextResponse.json({ error: `품목 추가 실패: ${error.message}` }, { status: 500 })
    }

    if (requested.length) {
      // 이미 pending 인 건은 unique 인덱스에 막힌다. 그건 실패가 아니라 정상이다.
      const { error } = await db.from('product_requests').insert(
        requested.map(pid => ({
          restaurant_id: restaurantId, product_id: pid, requested_by: userId,
        })), { ignoreDuplicates: true })
      if (error && error.code !== '23505') {
        return NextResponse.json({ error: `요청 등록 실패: ${error.message}` }, { status: 500 })
      }
    }

    return NextResponse.json({ added, requested })
  } catch (e) {
    console.error('[POST /api/member/products]', e)
    return NextResponse.json({ error: '품목을 추가하지 못했습니다.' }, { status: 500 })
  }
}

export async function DELETE(req: NextRequest) {
  try {
    const db = createAdminClient()
    const userId = await resolveUserId(req, db)
    if (!userId) return NextResponse.json({ error: '로그인이 필요합니다.' }, { status: 401 })

    const restaurantId = req.nextUrl.searchParams.get('restaurantId') ?? ''
    const productId = req.nextUrl.searchParams.get('productId') ?? ''
    if (!restaurantId || !productId) {
      return NextResponse.json({ error: '품목 정보가 없습니다.' }, { status: 400 })
    }

    const restaurant = await assertAccess(db, userId, restaurantId)
    if (!restaurant) return NextResponse.json({ error: '권한이 없습니다.' }, { status: 403 })

    const { data: row } = await db
      .from('restaurant_products').select('id, added_by')
      .eq('restaurant_id', restaurantId).eq('product_id', productId).maybeSingle()
    if (!row) return NextResponse.json({ error: '목록에 없는 품목입니다.' }, { status: 404 })

    if (!canMemberRemove(row.added_by ?? '')) {
      return NextResponse.json(
        { error: '담당자가 등록한 기본 품목은 뺄 수 없습니다.' }, { status: 403 })
    }

    const { error } = await db.from('restaurant_products').delete().eq('id', row.id)
    if (error) return NextResponse.json({ error: `삭제 실패: ${error.message}` }, { status: 500 })

    return NextResponse.json({ success: true })
  } catch (e) {
    console.error('[DELETE /api/member/products]', e)
    return NextResponse.json({ error: '품목을 빼지 못했습니다.' }, { status: 500 })
  }
}
```

- [ ] **Step 2: 타입체크**

Run: `cd apps/web && pnpm exec tsc --noEmit`
Expected: 0건. `buildPriceMapByProduct` 의 실제 시그니처를 먼저 읽어 인자 순서가 맞는지 확인한다.

- [ ] **Step 3: 인증이 막히는지 확인한다**

dev 서버를 띄운 상태에서:

```bash
curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/api/member/products?restaurantId=x&businessDate=2026-08-10"
```

Expected: `401`

- [ ] **Step 4: 커밋**

```bash
cd /Users/buxx-macmini/Developer/fruitlife
git add apps/web/app/api/member/products/route.ts
git commit -m "feat: 회원 품목 추가·제거 API"
```

---

## Task 4: 회원 화면 「품목 추가」

**Files:**
- Create: `apps/web/app/member/order/AddProductPanel.tsx`
- Modify: `apps/web/app/member/order/OrderForm.tsx`

**Interfaces:**
- Consumes: `GET/POST/DELETE /api/member/products` (Task 3)
- Produces: 발주 화면에서 품목을 넣고 뺄 수 있다

- [ ] **Step 1: 패널 컴포넌트를 만든다**

`apps/web/app/member/order/AddProductPanel.tsx`:

```tsx
'use client'

import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'

/**
 * 발주 화면의 「품목 추가」.
 *
 * 내 목록에 없는 품목을 분류별로 보여 준다. 단가가 있으면 바로 열리고,
 * 없으면 요청으로 들어가 담당자 확인을 거친다 — 단가 없이 발주되면
 * 명세서가 0 원으로 나가기 때문이다.
 */

interface Addable {
  id: string
  standard_name: string
  category: string | null
  default_unit: string
  price: number | null
  needsApproval: boolean
}

const CATEGORY_LABELS: Record<string, string> = {
  vegetable: '채소', fruit: '과일', grain: '곡류', meat: '육류',
  seafood: '수산', dairy: '유제품', seasoning: '양념', etc: '기타',
}

const won = (n: number) => `${n.toLocaleString('ko-KR')}원`

export default function AddProductPanel({
  restaurantId, businessDate,
}: { restaurantId: string; businessDate: string }) {
  const router = useRouter()
  const [open, setOpen] = useState(false)
  const [loading, setLoading] = useState(false)
  const [rows, setRows] = useState<Addable[]>([])
  const [pending, setPending] = useState<{ product_id: string; standard_name: string }[]>([])
  const [picked, setPicked] = useState<Set<string>>(new Set())
  const [msg, setMsg] = useState('')

  useEffect(() => {
    if (!open) return
    setLoading(true)
    fetch(`/api/member/products?restaurantId=${restaurantId}&businessDate=${businessDate}`)
      .then(r => r.json())
      .then((d: { addable?: Addable[]; pending?: typeof pending; error?: string }) => {
        if (d.error) { setMsg(d.error); return }
        setRows(d.addable ?? [])
        setPending(d.pending ?? [])
      })
      .catch(() => setMsg('품목을 불러오지 못했습니다.'))
      .finally(() => setLoading(false))
  }, [open, restaurantId, businessDate])

  function toggle(id: string) {
    setPicked(prev => {
      const next = new Set(prev)
      if (next.has(id)) next.delete(id); else next.add(id)
      return next
    })
  }

  async function add() {
    if (!picked.size) return
    setLoading(true); setMsg('')
    try {
      const res = await fetch('/api/member/products', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ restaurantId, productIds: [...picked] }),
      })
      const d = await res.json() as { added?: string[]; requested?: string[]; error?: string }
      if (!res.ok) throw new Error(d.error ?? '추가 실패')
      const parts: string[] = []
      if (d.added?.length) parts.push(`${d.added.length}개 추가됨`)
      if (d.requested?.length) parts.push(`${d.requested.length}개는 담당자 확인 중`)
      setMsg(parts.join(' · '))
      setPicked(new Set())
      router.refresh()
      setOpen(false)
    } catch (e) {
      setMsg(e instanceof Error ? e.message : '추가 실패')
    } finally {
      setLoading(false)
    }
  }

  const byCategory = rows.reduce<Record<string, Addable[]>>((acc, p) => {
    const key = p.category ?? 'etc'
    ;(acc[key] ??= []).push(p)
    return acc
  }, {})

  if (!open) {
    return (
      <div className="px-4 py-3">
        {pending.length > 0 && (
          <p className="text-xs text-amber-600 mb-2">
            담당자 확인 중: {pending.map(p => p.standard_name).join(', ')}
          </p>
        )}
        <button
          type="button"
          onClick={() => setOpen(true)}
          className="w-full rounded-lg border border-dashed border-gray-300 text-gray-600 py-3 text-sm font-semibold hover:bg-gray-50"
        >
          + 품목 추가
        </button>
      </div>
    )
  }

  return (
    <div className="px-4 py-3 border-t border-gray-200 bg-gray-50">
      <div className="flex items-center justify-between mb-2">
        <span className="text-sm font-semibold text-gray-700">품목 추가</span>
        <button type="button" onClick={() => setOpen(false)} className="text-sm text-gray-400 hover:text-gray-600">닫기</button>
      </div>

      {msg && <p className="text-xs text-gray-600 mb-2">{msg}</p>}
      {loading && <p className="text-sm text-gray-400 py-4 text-center">불러오는 중...</p>}

      {!loading && rows.length === 0 && (
        <p className="text-sm text-gray-400 py-4 text-center">추가할 수 있는 품목이 없습니다.</p>
      )}

      <div className="space-y-3 max-h-96 overflow-y-auto">
        {Object.entries(byCategory).map(([cat, items]) => (
          <div key={cat}>
            <p className="text-xs font-semibold text-gray-500 mb-1">{CATEGORY_LABELS[cat] ?? cat}</p>
            <div className="space-y-1">
              {items.map(p => (
                <label key={p.id} className="flex items-center gap-2 bg-white rounded px-3 py-2 cursor-pointer">
                  <input type="checkbox" checked={picked.has(p.id)} onChange={() => toggle(p.id)} className="w-4 h-4" />
                  <span className="flex-1 text-sm text-gray-800 truncate">{p.standard_name}</span>
                  {p.price !== null
                    ? <span className="text-xs text-gray-500 tabular-nums">{won(p.price)}/{p.default_unit}</span>
                    : <span className="text-xs text-amber-600">단가 문의</span>}
                </label>
              ))}
            </div>
          </div>
        ))}
      </div>

      <button
        type="button"
        onClick={add}
        disabled={loading || picked.size === 0}
        className="mt-3 w-full rounded-lg bg-brand-600 text-white py-2.5 text-sm font-semibold hover:bg-brand-700 disabled:opacity-50"
      >
        선택한 {picked.size}개 추가
      </button>
      <p className="mt-2 text-xs text-gray-400">
        「단가 문의」 품목은 바로 열리지 않고 담당자 확인을 거칩니다.
      </p>
    </div>
  )
}
```

- [ ] **Step 2: OrderForm 에 패널을 붙인다**

`apps/web/app/member/order/OrderForm.tsx` 를 읽고 다음을 한다.

1. 파일 상단에 import 를 추가한다:

```tsx
import AddProductPanel from './AddProductPanel'
```

2. 품목 목록이 끝나고 제출 버튼이 시작되기 직전에 다음을 넣는다:

```tsx
<AddProductPanel restaurantId={restaurantId} businessDate={businessDate} />
```

3. `Product` 타입에 `added_by?: string` 을 더하고, 품목 줄에 회원 추가분일 때만 제거 버튼을 그린다:

```tsx
{product.added_by === 'member' && (
  <button
    type="button"
    title="내 목록에서 빼기"
    onClick={async () => {
      if (!window.confirm(`"${product.products?.standard_name}" 을(를) 내 발주 목록에서 뺄까요?\n지난 발주 기록은 그대로 남습니다.`)) return
      const res = await fetch(
        `/api/member/products?restaurantId=${restaurantId}&productId=${product.id}`,
        { method: 'DELETE' })
      if (res.ok) router.refresh()
      else {
        const d = await res.json().catch(() => ({})) as { error?: string }
        window.alert(d.error ?? '빼지 못했습니다.')
      }
    }}
    className="shrink-0 text-gray-300 hover:text-red-500 text-lg leading-none px-1"
  >
    ×
  </button>
)}
```

`router` 가 없으면 `const router = useRouter()` 를 추가한다 (`next/navigation`).

- [ ] **Step 3: 발주 페이지가 출처를 넘기게 한다**

`apps/web/app/member/order/page.tsx` 의 `restaurant_products` 조회에 `added_by` 를 더하고,
`OrderForm` 에 넘기는 품목 객체에 `added_by` 를 담는다. 현재 조회는 이렇다:

```tsx
    .from('restaurant_products')
    .select('product_id')
```

다음으로 바꾼다:

```tsx
    .from('restaurant_products')
    .select('product_id, added_by')
```

그리고 `whitelistIds` 를 만드는 부분 아래에 출처 맵을 만들어 품목에 붙인다:

```tsx
  const addedByOf = new Map((whitelist ?? []).map(w => [w.product_id, w.added_by ?? 'admin']))
```

`OrderForm` 에 넘기는 products 배열의 각 항목에 `added_by: addedByOf.get(p.id) ?? 'admin'` 를 더한다.

- [ ] **Step 4: 단가 0 경고를 넣는다**

`OrderForm.tsx` 의 제출 처리에서, 수량이 있는 품목 중 단가가 0 인 것이 있으면 확인을 받는다.
제출 함수 맨 앞(수량 검사 뒤)에 넣는다:

```tsx
    const zeroPriced = selected.filter(p => Number(prices[p.id] ?? 0) <= 0)
    if (zeroPriced.length > 0) {
      const names = zeroPriced.map(p => p.products?.standard_name ?? '품목').join(', ')
      const ok = window.confirm(
        `단가가 정해지지 않은 품목이 있습니다 — ${names}\n\n` +
        `그대로 발주하면 금액이 0원으로 잡힙니다. 담당자에게 확인해 주세요.\n계속할까요?`)
      if (!ok) return
    }
```

변수명(`selected`, `prices`, `p.products`)은 그 파일의 실제 이름에 맞춘다. 다르면 맞춰 고친다.

- [ ] **Step 5: 타입체크하고 화면을 확인한다**

Run: `cd apps/web && pnpm exec tsc --noEmit` → 0건

dev 서버에서 회원 세션으로 발주 화면을 열어 「+ 품목 추가」가 보이는지 확인한다.
회원 세션 만드는 법은 Task 6 에 적어 두었다.

- [ ] **Step 6: 커밋**

```bash
cd /Users/buxx-macmini/Developer/fruitlife
git add apps/web/app/member/order/AddProductPanel.tsx apps/web/app/member/order/OrderForm.tsx apps/web/app/member/order/page.tsx
git commit -m "feat: 회원 발주 화면에서 품목을 직접 넣고 빼기"
```

---

## Task 5: 관리자 「품목 요청」

**Files:**
- Create: `apps/web/app/api/admin/products/requests/route.ts`
- Create: `apps/web/app/admin/products/requests/page.tsx`
- Create: `apps/web/app/admin/products/requests/RequestPanel.tsx`
- Modify: `apps/web/app/admin/products/page.tsx`

**Interfaces:**
- Consumes: Task 1 의 `product_requests`
- Produces: `POST /api/admin/products/requests` body `{ requestId, action: 'approve' | 'reject' }`

- [ ] **Step 1: API 를 만든다**

`apps/web/app/api/admin/products/requests/route.ts`:

```ts
export const runtime = 'edge'

import { NextRequest, NextResponse } from 'next/server'

import { getAuthorizedAdminDb } from '@/lib/admin-member-user'
import { getSessionUser } from '@/lib/supabase/server'
import { buildPriceMapByProduct } from '@/lib/specs/sync'

/**
 * 회원이 올린 품목 요청을 승인하거나 거절한다.
 *
 * **단가가 없으면 승인하지 않는다.** 단가 없이 열어 주면 발주가 그대로 들어와
 * 명세서가 0 원으로 나간다 (2026-08-09 일회용 손장갑).
 * 승인은 restaurant_products 에 added_by='member' 로 넣는다 — 회원이 원해서 열린
 * 품목이므로 회원이 뺄 수 있어야 한다.
 */

export async function POST(req: NextRequest) {
  try {
    const { requestId, action } = await req.json() as {
      requestId?: string; action?: 'approve' | 'reject'
    }
    if (!requestId || (action !== 'approve' && action !== 'reject')) {
      return NextResponse.json({ error: '요청 정보가 올바르지 않습니다' }, { status: 400 })
    }

    const db = await getAuthorizedAdminDb()
    if (!db) return NextResponse.json({ error: '권한이 없습니다' }, { status: 403 })
    const { user } = await getSessionUser()

    const { data: reqRow } = await db
      .from('product_requests')
      .select('id, restaurant_id, product_id, status, restaurants(organization_id)')
      .eq('id', requestId)
      .maybeSingle()
    if (!reqRow) return NextResponse.json({ error: '요청을 찾을 수 없습니다' }, { status: 404 })
    if (reqRow.status !== 'pending') {
      return NextResponse.json({ error: '이미 처리된 요청입니다' }, { status: 409 })
    }

    if (action === 'approve') {
      const rest = Array.isArray(reqRow.restaurants) ? reqRow.restaurants[0] : reqRow.restaurants
      const today = new Date(Date.now() + 9 * 3600_000).toISOString().slice(0, 10)
      const { priceMap } = await buildPriceMapByProduct(
        db, [reqRow.product_id], today, rest?.organization_id ?? null)

      if (Number(priceMap[reqRow.product_id] ?? 0) <= 0) {
        return NextResponse.json(
          { error: '단가가 없어 승인할 수 없습니다. 품목 단가를 먼저 등록해 주세요.' },
          { status: 400 })
      }

      const { error } = await db.from('restaurant_products').insert({
        restaurant_id: reqRow.restaurant_id,
        product_id: reqRow.product_id,
        added_by: 'member',
        added_by_user: user?.id ?? null,
      })
      // 이미 들어가 있으면 그건 실패가 아니다
      if (error && error.code !== '23505') {
        return NextResponse.json({ error: `품목 추가 실패: ${error.message}` }, { status: 500 })
      }
    }

    const { error: updErr } = await db.from('product_requests').update({
      status: action === 'approve' ? 'approved' : 'rejected',
      decided_at: new Date().toISOString(),
      decided_by: user?.id ?? null,
    }).eq('id', requestId)
    if (updErr) return NextResponse.json({ error: `상태 변경 실패: ${updErr.message}` }, { status: 500 })

    return NextResponse.json({ success: true, action })
  } catch (e) {
    console.error('[POST /api/admin/products/requests]', e)
    return NextResponse.json(
      { error: e instanceof Error ? e.message : '처리 중 오류가 발생했습니다' },
      { status: 500 })
  }
}
```

- [ ] **Step 2: 관리자 화면을 만든다**

`apps/web/app/admin/products/requests/page.tsx`:

```tsx
export const runtime = 'edge'

import { requireAuthorizedAdminDb } from '@/lib/admin-member-user'
import { fetchAll } from '@/lib/supabase/fetch-all'
import RequestPanel from './RequestPanel'

/**
 * 회원이 올린 품목 요청 목록.
 *
 * 단가가 없어서 바로 열지 못한 것들이 여기 쌓인다. 별도 알림은 두지 않았으므로
 * 관리자가 이 화면을 봐야 한다. 회원은 그동안 「담당자 확인 중」으로 보인다.
 */

export interface RequestRow {
  id: string
  orgName: string
  productName: string
  requestedAt: string
}

export default async function ProductRequestsPage() {
  const db = await requireAuthorizedAdminDb()

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const rows = await fetchAll<any>(() => db
    .from('product_requests')
    .select('id, requested_at, products(standard_name), restaurants(organizations(name))')
    .eq('status', 'pending')
    .order('requested_at', { ascending: false }))

  const list: RequestRow[] = rows.map(r => {
    const prod = Array.isArray(r.products) ? r.products[0] : r.products
    const rest = Array.isArray(r.restaurants) ? r.restaurants[0] : r.restaurants
    const org = Array.isArray(rest?.organizations) ? rest.organizations[0] : rest?.organizations
    return {
      id: r.id,
      orgName: org?.name ?? '알 수 없음',
      productName: prod?.standard_name ?? '품목',
      requestedAt: new Date(r.requested_at).toLocaleDateString('ko-KR', {
        timeZone: 'Asia/Seoul', month: '2-digit', day: '2-digit',
      }),
    }
  })

  return (
    <div className="p-5 max-w-3xl">
      <h1 className="text-base font-bold text-gray-900 mb-3">품목 요청 {list.length}건</h1>
      <RequestPanel rows={list} />
    </div>
  )
}
```

`apps/web/app/admin/products/requests/RequestPanel.tsx`:

```tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'

import type { RequestRow } from './page'

export default function RequestPanel({ rows }: { rows: RequestRow[] }) {
  const router = useRouter()
  const [busy, setBusy] = useState<string | null>(null)
  const [msg, setMsg] = useState('')

  async function decide(id: string, action: 'approve' | 'reject') {
    if (action === 'reject' && !window.confirm('이 요청을 거절할까요?')) return
    setBusy(id); setMsg('')
    try {
      const res = await fetch('/api/admin/products/requests', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ requestId: id, action }),
      })
      const d = await res.json() as { error?: string }
      if (!res.ok) throw new Error(d.error ?? '처리 실패')
      router.refresh()
    } catch (e) {
      setMsg(e instanceof Error ? e.message : '처리 실패')
    } finally {
      setBusy(null)
    }
  }

  if (rows.length === 0) {
    return (
      <div className="bg-white rounded-xl border border-gray-200 py-12 text-center text-sm text-gray-400">
        대기 중인 품목 요청이 없습니다
      </div>
    )
  }

  return (
    <div className="space-y-2">
      {msg && (
        <p className="rounded-lg bg-red-50 border border-red-200 px-4 py-2.5 text-sm text-red-700">{msg}</p>
      )}
      <div className="bg-white rounded-xl border border-gray-200 divide-y divide-gray-100">
        {rows.map(r => (
          <div key={r.id} className="flex items-center gap-3 px-5 py-3">
            <div className="flex-1 min-w-0">
              <div className="text-sm text-gray-900 truncate">{r.orgName}</div>
              <div className="text-xs text-gray-400">{r.productName} · 요청 {r.requestedAt}</div>
            </div>
            <button
              type="button"
              onClick={() => decide(r.id, 'approve')}
              disabled={busy === r.id}
              className="rounded-lg bg-brand-600 text-white px-4 py-1.5 text-xs font-semibold hover:bg-brand-700 disabled:opacity-50"
            >
              승인
            </button>
            <button
              type="button"
              onClick={() => decide(r.id, 'reject')}
              disabled={busy === r.id}
              className="rounded-lg border border-gray-300 text-gray-600 px-4 py-1.5 text-xs font-semibold hover:bg-gray-50 disabled:opacity-50"
            >
              거절
            </button>
          </div>
        ))}
      </div>
      <p className="text-xs text-gray-400 px-1">
        단가가 없는 품목은 승인되지 않습니다. 품목 관리에서 단가를 먼저 등록해 주세요.
      </p>
    </div>
  )
}
```

- [ ] **Step 3: 품목 관리에서 들어갈 수 있게 한다**

`apps/web/app/admin/products/page.tsx` 상단에 대기 건수와 링크를 넣는다. 그 파일의
기존 헤더 부분을 읽고 같은 모양으로 다음을 추가한다:

```tsx
<Link
  href="/admin/products/requests"
  className="rounded-lg border border-gray-300 text-gray-700 px-4 py-2 text-sm font-semibold hover:bg-gray-50"
>
  품목 요청{pendingCount > 0 ? ` ${pendingCount}` : ''}
</Link>
```

`pendingCount` 는 그 페이지에서 조회한다:

```tsx
const { count: pendingCount } = await db
  .from('product_requests')
  .select('id', { count: 'exact', head: true })
  .eq('status', 'pending')
```

- [ ] **Step 4: 타입체크하고 커밋**

Run: `cd apps/web && pnpm exec tsc --noEmit` → 0건

```bash
cd /Users/buxx-macmini/Developer/fruitlife
git add apps/web/app/api/admin/products/requests apps/web/app/admin/products/requests apps/web/app/admin/products/page.tsx
git commit -m "feat: 관리자 품목 요청 승인·거절"
```

---

## Task 6: 로컬 검증

**Files:** 없음 (검증만)

**Interfaces:**
- Consumes: Task 1~5 전부

> **실거래 데이터를 쓰지 않는다.** 모든 검증은 테스트 식당으로만 한다.
> `restaurant_id = 7306bb64-4596-4a5e-92ab-6e9dd9395238`
> 검증에 넣은 `restaurant_products` / `product_requests` 행은 끝나고 전부 지운다.

- [ ] **Step 1: 원본을 기록한다**

```sql
select count(*) as 테스트식당_품목수 from restaurant_products
where restaurant_id = '7306bb64-4596-4a5e-92ab-6e9dd9395238';
select count(*) as 요청건수 from product_requests;
```

- [ ] **Step 2: 세션을 만든다**

회원 세션(테스트 식당 소속 계정) 과 어드민 세션이 필요하다. 만드는 법:

```js
// service role 로 링크를 만들고 anon 으로 검증해 세션을 얻는다
const { data: link } = await admin.auth.admin.generateLink({ type: 'magiclink', email })
const { data: s } = await anon.auth.verifyOtp({ token_hash: link.properties.hashed_token, type: 'email' })
// 쿠키: sb-nvpaggacvbotgqyxfdof-auth-token = 'base64-' + base64url(JSON.stringify({
//   access_token, refresh_token, expires_at, expires_in, token_type, user }))
```

어드민 계정은 `82sh.kim@gmail.com`.
테스트 식당 소속 회원 계정은 `memberships` 에서 `organization_id = af6378e4-0000-0000-0000-000000000001` 로 찾는다. 없으면 어드민 계정으로 대신 확인하고 그 사실을 보고한다.

- [ ] **Step 3: 회원이 단가 있는 품목을 추가한다 (검증 1)**

발주 화면에서 「+ 품목 추가」 → 금액이 붙은 품목 하나를 골라 추가한다.
확인:

```sql
select p.standard_name, rp.added_by
from restaurant_products rp join products p on p.id = rp.product_id
where rp.restaurant_id = '7306bb64-4596-4a5e-92ab-6e9dd9395238' and rp.added_by = 'member';
```

Expected: 방금 고른 품목이 `added_by = 'member'` 로 있다. 발주 화면에도 뜬다.

- [ ] **Step 4: 단가 없는 품목을 고른다 (검증 2)**

「단가 문의」로 표시된 품목을 골라 추가한다.
확인:

```sql
select p.standard_name, r.status
from product_requests r join products p on p.id = r.product_id
where r.restaurant_id = '7306bb64-4596-4a5e-92ab-6e9dd9395238';

select count(*) as 발주목록에들어갔나
from restaurant_products rp join products p on p.id = rp.product_id
where rp.restaurant_id = '7306bb64-4596-4a5e-92ab-6e9dd9395238'
  and p.standard_name = '<고른 품목명>';
```

Expected: `product_requests` 에 `pending` 으로 있고, `restaurant_products` 에는 **없다**.
회원 화면에 「담당자 확인 중」이 보인다.

- [ ] **Step 5: 단가 없이 승인해 본다 (검증 6 의 앞단)**

어드민 「품목 요청」에서 그 건을 승인한다.

Expected: `단가가 없어 승인할 수 없습니다` 오류. `restaurant_products` 에 안 들어간다.

- [ ] **Step 6: 단가를 넣고 승인한다 (검증 3)**

품목 관리에서 그 품목에 단가를 등록한 뒤 다시 승인한다.

Expected: 승인되고 `restaurant_products` 에 `added_by='member'` 로 들어간다.
`product_requests.status = 'approved'`.

- [ ] **Step 7: 회원이 자기 품목을 뺀다 (검증 4·5)**

발주 화면에서 회원 추가분의 `×` 를 눌러 뺀다 → 목록에서 사라진다.
관리자가 넣은 기본 품목에는 `×` 가 **없는지** 확인한다.

API 로도 직접 시험한다 (기본 품목은 막혀야 한다):

```bash
curl -s -X DELETE -H "Cookie: $MEMBER_COOKIE" \
  "http://localhost:3000/api/member/products?restaurantId=7306bb64-4596-4a5e-92ab-6e9dd9395238&productId=<기본품목id>"
```

Expected: `403` `담당자가 등록한 기본 품목은 뺄 수 없습니다.`

- [ ] **Step 8: 같은 품목을 두 번 요청한다 (검증 7)**

이미 요청한 품목을 또 고른다.

Expected: 목록에 아예 안 보인다(`buildAddable` 이 거른다). API 로 직접 두 번 보내도
`product_requests` 행이 하나만 남는다.

- [ ] **Step 9: 단가 0 발주 경고를 확인한다 (검증 6)**

단가가 없는 품목을 (승인 전 상태로) 발주 목록에 억지로 넣을 수는 없으므로,
단가가 0 인 상황을 만들어 확인한다. 테스트 식당 품목 하나의 단가를 지우고 발주를 시도한다.
경고창이 뜨는지 보고, 확인 후 단가를 되돌린다.

- [ ] **Step 10: 다른 업체 품목이 새지 않는지 확인한다 (검증 8)**

회원 세션으로 다른 업체 `restaurantId` 를 넣어 호출한다:

```bash
curl -s -o /dev/null -w "%{http_code}\n" -H "Cookie: $MEMBER_COOKIE" \
  "http://localhost:3000/api/member/products?restaurantId=06d64b75-ec2d-492a-aad0-3870e4a82e4c&businessDate=2026-08-10"
```

Expected: `403`

- [ ] **Step 11: 검증에 넣은 것을 전부 지운다**

```sql
delete from restaurant_products
where restaurant_id = '7306bb64-4596-4a5e-92ab-6e9dd9395238' and added_by = 'member';

delete from product_requests
where restaurant_id = '7306bb64-4596-4a5e-92ab-6e9dd9395238';
```

확인:

```sql
select
  (select count(*) from restaurant_products
    where restaurant_id='7306bb64-4596-4a5e-92ab-6e9dd9395238') as 품목수,
  (select count(*) from restaurant_products
    where restaurant_id='7306bb64-4596-4a5e-92ab-6e9dd9395238' and added_by<>'admin') as 회원추가분,
  (select count(*) from product_requests) as 요청건수;
```

Expected: 품목수 = Step 1 값, 회원추가분 0, 요청건수 = Step 1 값

- [ ] **Step 12: 결과를 보고한다**

검증 8항목 각각의 결과를 사실대로 보고한다. 통과하지 못한 항목이 있으면 그대로 말한다.
Step 11 의 원복 확인 결과를 함께 보고한다.

---

## Self-Review

**1. 스펙 커버리지**

| 스펙 요구 | 담당 과제 |
|---|---|
| `restaurant_products` 출처 컬럼 | Task 1 |
| `product_requests` 표 + pending unique | Task 1 |
| 단가 있으면 즉시 / 없으면 요청 | Task 2(판정), Task 3(POST) |
| 단가 0 을 "없음" 으로 취급 | Task 2 |
| 회원 화면 「품목 추가」 (분류별, 단가 표시) | Task 4 |
| 「담당자 확인 중」 표시 | Task 4 |
| 회원 추가분만 `×` | Task 3(DELETE 권한), Task 4(화면) |
| 관리자 「품목 요청」 화면 | Task 5 |
| 단가 없으면 승인 불가 | Task 5 |
| 승인 시 `added_by='member'` | Task 5 |
| 단가 0 발주 경고 | Task 4 Step 4 |
| 검증 8항목 | Task 6 |
| 앱 제외 · 알림 제외 · 검색 제외 | 어느 과제에도 없음 (의도) |

빠진 요구 없음.

**2. 플레이스홀더** — 없음. 모든 코드 단계에 실제 코드가 들어 있다.

**3. 타입 일관성**

- `CatalogProduct` / `AddableProduct` 는 Task 2 에서 정의하고 Task 3 이 import 한다.
- Task 4 의 `Addable` 인터페이스는 Task 3 의 GET 응답 형태와 필드가 같다
  (`id, standard_name, category, default_unit, price, needsApproval`).
- `RequestRow` 는 Task 5 의 `page.tsx` 에서 export 하고 `RequestPanel.tsx` 가 import 한다.
- `added_by` 는 DB(Task 1) → API(Task 3) → 화면(Task 4) 에서 모두 `'admin' | 'member'` 문자열이다.
- Task 3·5 가 쓰는 `buildPriceMapByProduct(db, productIds, businessDate, organizationId)` 는
  기존 `lib/specs/sync.ts` 의 함수다. 각 과제 첫 단계에서 실제 시그니처를 확인하도록 적어 두었다.
